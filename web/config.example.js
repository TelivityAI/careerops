// Copy to config.js and fill in YOUR project values.
// config.js is gitignored — never commit real keys to the public repo.
window.CAREEROPS_CONFIG = {
  supabaseUrl: 'https://YOUR_PROJECT.supabase.co',
  supabaseAnonKey: 'YOUR_SUPABASE_ANON_OR_PUBLISHABLE_KEY',
  // optional
  donateUrl: ''
}
