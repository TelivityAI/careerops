# Secrets — never commit
.hf_token
hf_token
**/hf_token
*.key
.anthropic_key
.gemini_key
.kimi_key
.env
.env.*
!.env.example
**/secrets/**
kaggle.json
.sb_token
.sb-*.txt

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editors
.DS_Store
.idea/
.vscode/
*.swp

# Training artifacts (large / local only)
*.bin
*.safetensors
*.gguf
*.pt
*.pth
*.ckpt
wandb/
runs/
outputs/
checkpoints/
adapter_*/
merged/

# Rejected gen rows — keep out of train merges; optional to track empty dir
# data/clean/_rejected/ contents may be large; ignore generated rejects
data/clean/_rejected/**
!data/clean/_rejected/.gitkeep

# Local HF / Kaggle caches
.cache/
huggingface/
.venv-probe/

# Operator / chat / product-ops — never ship publicly
# Local park for private ops notes is .operator/
.operator/
.fake-home/
.supabase-cli-home/
docs/_internal/
career-ops-ux/
supabase/
.kaggle_upload_train/

# Local deploy secrets for the web app
web/config.js

# Editor / agent backups — never ship
*.bak*
*.bak-*
*.broken*
web/*.bak*
web/*.preview*

# Training/eval — private vault only, never re-add to public
data/
eval/
evalkernel/
kernel/

# Never re-add train tooling to public
scripts/assemble*.py
scripts/assembler.py
scripts/audit_*.py
scripts/demo_infer.py
scripts/merge_*.py
scripts/preflight_corpus.sh
scripts/quickstart.sh
scripts/run_privacy_probe.py
scripts/validate_clean.py
scripts/privacy/
requirements-infer.txt
packages/**/*.tgz
